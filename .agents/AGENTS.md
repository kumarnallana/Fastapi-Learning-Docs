# Git Commit Instructions

Whenever the user asks to "provide a git commit" or for a git commit message, you MUST provide the complete set of commands to stage, commit, and push the changes:

```bash
git add .
git commit -m "feat/fix: <title>" -m "<description>"
git push
```
Ensure you provide a clear and descriptive commit message based on the recent changes.
